future toDo List:

1) Database per lo storico, più capiente, anche meno performante, visibile solo da lato amministratore, dove contere la cronologia di tutti i flight passati, utile per statistiche e marketing. Il db principale contiene solo i voli futuri non effettuati, il server, ogni volta che un volo parte, lo elimina dal db principale e lo sposta sul db dello storico, (oppure giornalmente, a discrezione dell'utente)

2)All'avvio del client, esso deve fare una verifica per vedere se si trova all'ultima versione(per la congruenza delle classi), altrimenti non è avviabile finché non si installa la nuova versione, questo perché abbiamo previsto una comunicazione Json tra client e server. Altrimenti prevedere una gestione del client dinamica (-> molto avanzata)

3)Pagamenti più ottimizzato, carte di credito.

4)Prevedere un db(o un interfacciamento) contabile, accessibile solo da amministratore.

